bin/
obj/
.vs/

# Build output: installers, portable publishes, WiX debug symbols
publish/
*.msi
*.msix
*.wixpdb
*.pfx

# Test images are large + not ours to redistribute — keep only the README.
testdata/*
!testdata/README.md

# Spike scratch output (posters, benchmark CSVs, import runs)
out_spike*/
spikes/
RAWimp.Spikes.slnx
